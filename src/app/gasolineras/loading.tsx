import LoadingGasolineras from "@/components/layout/LoadingPagina";

/** Loading del índice de gasolineras (España). */
export default function Loading() {
  return <LoadingGasolineras nivel="pagina" />;
}
