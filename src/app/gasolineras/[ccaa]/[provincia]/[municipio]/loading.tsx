import LoadingGasolineras from "@/components/layout/LoadingPagina";

/** Loading de páginas de municipio (/gasolineras/[ccaa]/[provincia]/[municipio]). */
export default function Loading() {
  return <LoadingGasolineras nivel="municipio" />;
}
