import LoadingGasolineras from "@/components/layout/LoadingPagina";

/** Loading de páginas de provincia (/gasolineras/[ccaa]/[provincia]). */
export default function Loading() {
  return <LoadingGasolineras nivel="provincia" />;
}
