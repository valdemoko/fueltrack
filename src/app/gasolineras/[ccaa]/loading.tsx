import LoadingGasolineras from "@/components/layout/LoadingPagina";

/** Loading de páginas de comunidad autónoma (/gasolineras/[ccaa]). */
export default function Loading() {
  return <LoadingGasolineras nivel="ccaa" />;
}
